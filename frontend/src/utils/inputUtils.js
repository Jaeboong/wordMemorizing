/**
 * 입력 필드 언어 설정 관련 유틸리티 함수
 */

// 문서 로드 후 모든 입력 필드에 언어 관련 스타일 적용
export const setupInputLanguageSupport = () => {
  // index.html에 이미 구현되어 있으므로 추가 코드 필요 없음
};

// CSS 스타일 추가
export const addLanguageStyles = () => {
  // index.html에 이미 구현되어 있으므로 추가 코드 필요 없음
}; 